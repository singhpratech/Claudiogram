{
  "name": "claudiogram",
  "version": "1.0.0",
  "description": "Personal Claude Code usage observatory — live token pulse, heatmaps, cost story, time capsule",
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node server.js",
    "ingest": "node --input-type=module -e \"import('./lib/ingest.js').then(m=>m.fullScan().then(r=>console.log(r)))\"",
    "test": "node --test \"test/*.test.mjs\""
  },
  "license": "MIT"
}
